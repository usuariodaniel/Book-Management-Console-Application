CREATE TABLE autores (
                         id BIGINT AUTO_INCREMENT,
                         nome VARCHAR(255),
                         data_de_nascimento DATE,
                         data_de_falecimento DATE,
                         PRIMARY KEY(id)
);